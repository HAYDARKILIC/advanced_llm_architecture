# Repository Identity Card

This file documents the GitHub metadata for the repository. Copy the values below
into the GitHub repository settings when publishing.

**Course:** *Advanced LLM Architectures & Optimization* (six-week graduate curriculum).
**Anchor texts:** Raschka (2024), *Build a Large Language Model (From Scratch)*; Bishop &
Bishop (2024), *Deep Learning: Foundations and Concepts*.

---

## Repository Name

```
bitwise-llm-forge
```

Alternative names considered (in descending preference):
- `bitwise-llm-forge` — chosen: evocative, unique, hints at low-level bit manipulation
- `forge-llm`
- `ternary-transformer-lab`
- `minllm-from-scratch`

---

## Short Description (≤ 120 chars, shown on profile)

```
Forging hardware-aware LLMs from first principles: BitNet b1.58, TTT layers, FlashAttention tiling, RoPE — all from scratch in PyTorch.
```

Alternative one-liners:
- *"A graduate-level, from-scratch implementation of modern LLM optimization techniques in pure PyTorch."*
- *"Ternary weights, rotary embeddings, tiled attention, and test-time training — derived, implemented, benchmarked."*

---

## Topics / Tags

Add these in GitHub → repository → ⚙️ → Topics:

```
llm
large-language-models
pytorch
quantization
bitnet
ternary-weights
flash-attention
rope
rotary-embeddings
test-time-training
differentiable-logic
from-scratch
deep-learning
transformer
attention-mechanism
gpu-optimization
machine-learning
ai-research
educational
production-grade
```

---

## Suggested GitHub "About" panel website link

```
https://github.com/HAYDARKILIC/bitwise-llm-forge#readme
```

---

## Social-preview / OG image

A 1280×640 PNG should be added at `docs/figures/social_preview.png` once produced.
Recommended visual: a black background, a 3D-rendered ternary weight matrix
( -1 / 0 / +1 cells in three distinct colors) with the title
**BitWise-LLM-Forge** in monospace.
